{"assertions":["main body renders the two-line remittance table","the FOOTER_NOTE subtemplate content ('Payment terms: net 30...') renders once under the table because SHOW_NOTE=Y wraps the call","the <?import:?> stays unconditional at the top of the body"],"note":"data file is data.xml (NOT sample.xml) on purpose: the main harness drops imports[] and sends no subtemplates, so an unresolved call-template would be a guaranteed XSL static error (XML-22049 template not found) - a misleading FAIL. The harness therefore SKIPs this row; verify-extras.mts renders main+sub end-to-end via bip-render's subtemplates[] contract (sub source: sub.json)."}
