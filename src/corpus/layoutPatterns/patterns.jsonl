{"id": "lp:archetype:statement-run", "kind": "archetype", "name": "Statement run (page-per-master dossier)", "format": "rtf", "dslSupport": "supported", "description": "One page (or page group) per master entity: master name as the page title, master-level facts under it, then each child entity's own section with a lines table. The workhorse for customer statements, per-supplier dossiers, per-employee packets.", "whenToUse": "The ask contains 'one page per X', 'X name as page header/title', or child entities grouped under a master. Decide BEFORE the data model: this archetype demands NESTED output groups — flat data cannot feed it.", "intents": ["one page per customer with their invoices and lines", "each supplier on its own page with details grouped under it", "customer name as page title, invoices listed below with line items", "statement per account with transactions grouped by document"], "requires": {"dataShape": "nested datamodel output groups MASTER > CHILD > LINE via layered groupBy (e.g. G_CUSTOMER > G_INVOICE > G_LINE); ORDER BY the group columns in SQL", "preAggregation": "master-level totals as SQL columns on the master group where possible (SUM OVER / ROLLUP), not template math"}, "composition": {"contains": ["lp:technique:rtf-multilevel-blocks", "lp:technique:rtf-totals-row"], "notes": "combine with a page header/footer technique for page furniture; chart-on-first-page composes as a TOP-LEVEL chart block before the outer forEach"}, "recipe": {"format": "rtf", "blocks": [{"kind": "forEach", "group": "/DATA_DS/G_MASTER", "splitByPage": true, "blocks": [{"kind": "heading", "level": 1, "runs": [{"field": "MASTER_NAME"}]}, {"kind": "paragraph", "runs": ["Total due: ", {"field": "MASTER_TOTAL", "format": {"type": "currency", "mask": "#,##0.00"}}]}, {"kind": "forEach", "group": "G_CHILD", "blocks": [{"kind": "heading", "level": 2, "runs": ["Document ", {"field": "CHILD_NUMBER"}, " - ", {"field": "CHILD_DATE", "format": {"type": "date", "mask": "dd-MMM-yyyy"}}]}, {"kind": "table", "forEach": "G_LINE", "border": true, "headerBg": "#cfe0f1", "columns": [{"header": "Line", "field": "LINE_NUMBER"}, {"header": "Description", "field": "LINE_DESCRIPTION"}, {"header": "Amount", "field": "LINE_AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}]}]}]}]}, "fixtureRef": "fixtures/statement-run", "pitfalls": ["Inner forEach/table groups are RELATIVE (no leading /) — an absolute inner group is rejected by the builder.", "Never fake depth from flat data with ../FIELD + preceding-sibling guards — renders a collapsed table (verified failure). Restructure the data model instead.", "Bind placeholder names (G_MASTER, MASTER_NAME) to YOUR datamodel's actual output tree before building."], "sourceRefs": {"verifiedIn": "live E2E 2026-08-05: 119-page pod render, pie page 1 + per-customer sections"}, "verified": "render"}
{"id": "lp:technique:rtf-multilevel-blocks", "kind": "technique", "name": "Multi-level nested sections (blocks[] forEach nesting)", "format": "rtf", "dslSupport": "supported", "description": "Three-plus level master-detail inside one RTF: nested forEach blocks mirror the datamodel's nested groups; each level adds headings/facts; the innermost level is a lines table.", "whenToUse": "Any hierarchy deeper than the simple invoice shape (one master + one lines level). The data model MUST provide the same nesting via groupBy.", "intents": ["customer then invoices then invoice lines nested sections", "three level grouping in one document", "master detail detail report structure", "sections inside sections with their own tables"], "requires": {"dataShape": "nested output groups matching every forEach level"}, "composition": {"nestsIn": ["lp:archetype:statement-run"], "notes": "splitByPage:true on the OUTER forEach = page break per master; on a table block it means one page per ROW — different semantics"}, "recipe": {"blocks": [{"kind": "forEach", "group": "/DATA_DS/G_A", "splitByPage": true, "blocks": [{"kind": "heading", "level": 1, "runs": [{"field": "A_NAME"}]}, {"kind": "forEach", "group": "G_B", "blocks": [{"kind": "heading", "level": 2, "runs": [{"field": "B_TITLE"}]}, {"kind": "table", "forEach": "G_C", "columns": [{"header": "Item", "field": "C_NAME"}, {"header": "Qty", "field": "C_QTY", "align": "right"}]}]}]}]}, "fixtureRef": "fixtures/multilevel", "pitfalls": ["Inner groups RELATIVE; outer absolute.", "heading/paragraph text is runs[] — literals and {field,format?} objects; there is NO {FIELD} string interpolation.", "The page-break knob is splitByPage — there is no pageBreakAfter."], "sourceRefs": {"verifiedIn": "user web test rerun 2026-08-04: 417-page render, 12 customers on fresh pages"}, "verified": "render"}
{"id": "lp:technique:rtf-totals-row", "kind": "technique", "name": "Table totals row (sum/count with label span and shading)", "format": "rtf", "dslSupport": "supported", "description": "A bold totals row under a lines table: label cell (optionally spanning several leading columns) plus aggregate cells; optional background shade.", "whenToUse": "Any lines table where the user asks for totals/subtotals per section. Prefer SQL/rollup columns for GRAND totals across sections.", "intents": ["totals row under the invoice lines table", "sum the amount column at the bottom", "subtotal per section with shaded totals line"], "composition": {"nestsIn": ["lp:technique:rtf-multilevel-blocks", "lp:archetype:statement-run"]}, "recipe": {"tableFragment": {"kind": "table", "forEach": "G_LINE", "columns": [{"header": "Description", "field": "DESC"}, {"header": "Qty", "field": "QTY", "align": "right"}, {"header": "Amount", "field": "AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}], "totals": [{"col": 0, "label": "Total", "span": 2}, {"col": 2, "expr": "format-number:sum(G_LINE/AMOUNT);'#,##0.00'"}], "totalsBg": "#f7cbac"}}, "fixtureRef": "fixtures/totals-row", "pitfalls": ["TotalCell shape is {col, label|expr, span?} — col is the 0-based column index; span merges LEADING label cells.", "A total that must cross sections (grand total) belongs in SQL (ROLLUP/SUM OVER), not in the template."], "sourceRefs": {"verifiedIn": "packing-list E2E 2026-08-03 (correct-scope totals)"}, "verified": "render"}
{"id": "lp:technique:rtf-chart", "kind": "technique", "name": "RTF chart block (pie/bar/line, grouped or raw)", "format": "rtf", "dslSupport": "supported", "description": "A real vector chart inside an RTF template: shape-alt-text Graph encoding rendered by the BIP engine. Grouped mode aggregates a measure per category; raw mode plots one bar per row.", "whenToUse": "'Add a chart/pie/graph' to a print report or its first page. TOP-LEVEL placement only.", "intents": ["pie chart of amount by customer on the first page", "bar chart of totals per department in the pdf", "add a graph above the table in the report"], "composition": {"notes": "place BEFORE any forEach; chart inside a forEach re-emits per iteration (verified duplication)"}, "recipe": {"kind": "chart", "graphType": "PIE", "title": "Amount by Category", "group": {"select": "/DATA_DS/G_ROW", "by": "CATEGORY_NAME"}, "measures": [{"label": "Amount", "field": "AMOUNT", "agg": "sum"}], "dataLabels": true, "size": {"widthPx": 500, "heightPx": 375}}, "fixtureRef": "fixtures/rtf-chart", "pitfalls": ["Keys are graphType / group:{select,by} / measures:[{label,field,agg:sum|count|avg}] — NOT the XPT vocabulary (type/dimensionField/string group); the builder rejects wrong keys with a teaching error.", "A select matching 0 nodes renders a normal-looking frame with literal 'No data to display' at HTTP 200 — verify group.select against the data XML and LOOK at the chart area after render.", "summarizeReportLayout/extractText cannot see charts (alt-text shape, no <?tags?>) — tagCount 0 is normal."], "sourceRefs": {"verifiedIn": "chartproof matrix + live pod E2E 2026-08-04 (pie page 1, real data)"}, "verified": "render"}
{"id": "lp:technique:xpt-grid-chart-table", "kind": "technique", "name": "XPT side-by-side: chart + repeating detail table (LayoutGrid)", "format": "xpt", "formatExclusive": true, "dslSupport": "supported", "description": "The classic monitoring-dashboard row: an interactive chart in the left cell and a REPEATING detail table in the right cell of one LayoutGrid row. Exports to a clean PDF too.", "whenToUse": "'Dashboard: chart on the left, details on the right'. This shape is XPT-ONLY — an RTF grid cell cannot host a repeating table (BIP collapses it).", "intents": ["dashboard with chart left and detail table right", "chart beside a repeating data table on one page", "monitoring dashboard side by side layout"], "requires": {"dataShape": "one detail-grain group feeds both: the chart aggregates it, the table lists it; charts want long/detail rows, not pre-aggregated wide columns"}, "composition": {"conflicts": ["rtf grid with repeating table in a cell"], "notes": "grid columns are PIXELS (sum ~1554 for a 1650px page); each chart binds EXACTLY 1 measure (multi-series via seriesField); DataTable filtering via filters[] — an XPath predicate yields 0 rows"}, "recipe": {"format": "xpt", "grid": {"pageWidth": 1650, "columns": [560, 1044], "rows": [{"cells": [{"chart": {"type": "PIE", "group": "G_ROW", "dimensionField": "STATUS", "measures": [{"name": "REQUEST_ID", "agg": "count"}]}}, {"table": {"group": "G_ROW", "fields": [{"name": "REQUEST_ID"}, {"name": "STATUS"}, {"name": "START_DATE"}], "filters": [{"field": "DAYS_AGO", "operator": "less_or_equal", "value": 1}]}}]}]}}, "fixtureRef": "fixtures/xpt-grid", "pitfalls": ["Column widths in PIXELS here — twips values (4200/7800) push cells off-page and render blank.", "2+ measures on one XPT chart faults when a sibling chart shares the page — builder rejects it; use seriesField.", "No local render for .xpt — verify on the pod (prepareReportForPod -> runReport).", "LayoutTable.fields entries are ReportField OBJECTS ({name, label?, dataType?}) � bare string field names fail xpt compile."], "sourceRefs": {"verifiedIn": "ESS dashboard matrix sessions; corpus exemplars (Oracle ESS dashboard)"}, "verified": "pod"}
{"id": "lp:anti:template-wide-pivot", "kind": "antipattern", "name": "Wide pivot computed in the template (per-cell XSLT sums)", "format": "rtf", "dslSupport": "n/a", "description": "Building a many-column pivot (column per SKU/month/period) as per-cell cross-group XSLT sums inside the template.", "whenToUse": "", "intents": ["pivot by SKU columns in the layout", "one column per month computed in the template", "matrix report built with template formulas per cell"], "trigger": "a pivot/matrix ask ('column per X') heading toward template-side computation", "why": "It renders, but tens of near-identical XSLT sum() cells are unmaintainable by a human (verified: the Tama Cotton File pivot) and silently break on Word edits — banned by the simplicity contract.", "instead": ["lp:archetype:pivot-report"], "alternative": "Pivot in SQL — native PIVOT or SUM(CASE WHEN key='…' THEN qty END) columns; the template then shows a flat table. For interactive use, XPT's native Crosstab is legitimate.", "verified": "prose"}
{"id": "lp:anti:parent-axis-fake-nesting", "kind": "antipattern", "name": "Faking hierarchy with ../FIELD + preceding-sibling guards", "format": "rtf", "dslSupport": "n/a", "description": "Emulating a 3-level document over FLAT data by reaching up with ../PARENT_FIELD and suppressing repeats with position()/preceding-sibling tricks.", "whenToUse": "", "intents": ["show parent fields inside a flat detail loop", "suppress repeating header values with preceding-sibling", "three level report from a flat dataset"], "trigger": "a multi-level ask being forced into the flat invoice shape (outerGroup/linesGroup) instead of nested groups", "why": "Collapses into a broken table — misaligned headers, repeated/blank cells (verified failure, user web test 2026-08-04). The builder now rejects the absolute-inner-group variant.", "instead": ["lp:archetype:statement-run", "lp:technique:rtf-multilevel-blocks"], "alternative": "Model the hierarchy in the DATA: layered datamodel groupBy (G_A > G_B > G_C), then nested forEach blocks.", "verified": "prose"}
{"id": "lp:cap:carry-forward-page-totals", "kind": "technique", "name": "Carry-forward / brought-forward page totals", "format": "rtf", "dslSupport": "unsupported", "description": "Running page totals printed at the bottom of each page ('carried forward') and repeated at the top of the next ('brought forward') — a standard BIP capability via <?add-page-total?>/<?show-page-total?> that our generator does not emit yet.", "whenToUse": "Financial listings that must show running totals per printed page. BIP supports it; our DSL has no block for it yet — say so honestly and offer the nearest alternative (per-section totals via the totals row, or a running SQL SUM OVER column).", "intents": ["carried forward brought forward totals on each page", "running total at the bottom of every printed page", "page totals that continue to the next page"], "pitfalls": ["Do NOT attempt to fake page totals with template math — page boundaries are a rendering concept invisible to XSLT."], "sourceRefs": {"atlas": "print-plumbing gap"}, "verified": "prose"}
{"id": "lp:archetype:invoice-document", "kind": "archetype", "name": "Invoice document (header band + lines table + totals)", "format": "rtf", "dslSupport": "supported", "description": "The classic transactional document: a bold label/value header band (customer, number, date, terms), a bordered line-items table, and a shaded totals row summing the lines. Batch runs put one document per page. Fits invoices, credit memos, packing lists, order confirmations.", "whenToUse": "The ask is a printable document per business transaction with identity fields on top and its lines below. TWO levels only (document > lines) - a third level (customer > invoice > lines) is lp:archetype:statement-run instead. Print/PDF/Word delivery -> RTF.", "intents": ["invoice layout with customer details on top and line items below", "packing list with header fields and a lines table", "credit memo with a totals row under the item table", "printable document per order showing its lines and total", "invoice header band then items then amount due"], "requires": {"dataShape": "one G_INVOICE node per document carrying the header fields, with nested G_LINE rows (datamodel groupBy); ORDER BY invoice then line in the SQL", "preAggregation": "any header-band total (amount due, tax) as a SQL column on G_INVOICE; the lines sum in the totals row is template-side sum(G_LINE/FIELD)"}, "composition": {"contains": ["lp:technique:rtf-totals-row"], "notes": "splitByPage:true on the outer forEach = one page per invoice; add page.header/footer blocks for running furniture; subtemplate chrome composes like lp:archetype:form-letter"}, "recipe": {"format": "rtf", "blocks": [{"kind": "forEach", "group": "/DATA_DS/G_INVOICE", "splitByPage": true, "blocks": [{"kind": "heading", "level": 1, "runs": ["Invoice ", {"field": "INVOICE_NUMBER"}]}, {"kind": "paragraph", "runs": [{"text": "Customer: ", "bold": true}, {"field": "CUSTOMER_NAME"}]}, {"kind": "paragraph", "runs": [{"text": "Invoice date: ", "bold": true}, {"field": "INVOICE_DATE", "format": {"type": "date", "mask": "dd-MMM-yyyy"}}]}, {"kind": "paragraph", "runs": [{"text": "Terms: ", "bold": true}, {"field": "PAYMENT_TERMS"}]}, {"kind": "table", "forEach": "G_LINE", "border": true, "headerBg": "#cfe0f1", "totalsBg": "#f7cbac", "columns": [{"header": "Line", "field": "LINE_NUMBER", "width": 900}, {"header": "Description", "field": "LINE_DESCRIPTION", "width": 3960}, {"header": "Qty", "field": "QUANTITY", "align": "right", "width": 1100}, {"header": "Unit Price", "field": "UNIT_PRICE", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right", "width": 1700}, {"header": "Amount", "field": "LINE_AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right", "width": 1700}], "totals": [{"col": 0, "label": "Total", "span": 4}, {"col": 4, "expr": "format-number:sum(G_LINE/LINE_AMOUNT);'#,##0.00'"}]}]}]}, "fixtureRef": "fixtures/invoice-document", "pitfalls": ["The lines table forEach is RELATIVE (G_LINE) under the outer forEach - an absolute inner path repeats every line of every invoice under each invoice and totals go global (see lp:anti:absolute-inner-group).", "Totals cells are {col, expr|label, span}; the sum path is relative to the invoice context: sum(G_LINE/LINE_AMOUNT).", "Bind placeholder names (G_INVOICE, LINE_AMOUNT) to YOUR datamodel output tree before building."], "sourceRefs": {"atlas": "repeat-group #3 + totals-and-aggregates #16", "verifiedIn": "shard-arch harness render 2026-08-06: 2 invoices, one page each, formatted totals"}, "verified": "render"}
{"id": "lp:archetype:monitoring-dashboard", "kind": "archetype", "name": "Monitoring dashboard (XPT: KPI cards + charts + filtered detail tables)", "format": "xpt", "dslSupport": "supported", "description": "The interactive operations dashboard as ONE .xpt layout stacked top to bottom: a KPI card row (headline numbers), interactive charts, then one or more FILTERED detail tables all fed by one detail-grain group. Exports to a clean PDF too.", "whenToUse": "'Dashboard showing X, Y, Z' viewed online (or exported). Build ONE layout - do not split sections into separate templates. Needs LONG detail rows. Side-by-side chart|table placement -> switch the stack to the grid form (lp:technique:xpt-grid-chart-table); print-first fixed geometry -> lp:archetype:print-dashboard.", "intents": ["monitoring dashboard with kpis charts and detail tables", "operations dashboard: headline numbers then status pie then last 24h table", "one dashboard page with a chart and several filtered views of the data", "ess style request monitor with error detail sections"], "requires": {"dataShape": "one detail-grain group (e.g. G_ROW: REQUEST_ID, STATUS, RUN_DAY, DAYS_AGO) feeds the charts AND the filtered tables; plus a single-row KPI group (G_KPI) whose headline totals are SQL columns", "preAggregation": "KPI values via SQL aggregates in a one-row dataset/group; charts aggregate the detail rows themselves (count/sum) - do NOT pre-pivot into wide columns"}, "composition": {"contains": ["lp:technique:xpt-grid-chart-table"], "notes": "flat stack order is fixed: kpis, then charts, then tables; a grid replaces the stack when side-by-side is wanted; every chart is wired for click-to-filter automatically"}, "recipe": {"label": "Monitoring Dashboard", "format": "xpt", "title": "Operations Monitor", "kpis": [{"label": "Total Requests", "field": "TOTAL_REQUESTS", "group": "G_KPI"}, {"label": "Errors (24h)", "field": "ERROR_COUNT", "group": "G_KPI"}], "charts": [{"type": "PIE", "title": "Requests by Status (24h)", "group": "G_ROW", "dimensionField": "STATUS", "measures": [{"name": "REQUEST_ID", "agg": "count"}], "filter": "DAYS_AGO <= 1", "size": {"width": 500, "height": 300}}, {"type": "BAR_VERT_STACK", "title": "Runs per Day by Status", "group": "G_ROW", "dimensionField": "RUN_DAY", "seriesField": "STATUS", "measures": [{"name": "REQUEST_ID", "agg": "count"}]}], "tables": [{"title": "Last 24h Detail", "group": "G_ROW", "filters": [{"field": "DAYS_AGO", "operator": "less_or_equal", "value": 1}], "fields": [{"name": "REQUEST_ID", "dataType": "number"}, {"name": "STATUS"}, {"name": "RUN_DAY"}], "totals": {"label": "Requests", "aggs": {"REQUEST_ID": "count"}}}, {"title": "Errors (7 days)", "group": "G_ROW", "filters": [{"field": "DAYS_AGO", "operator": "less_or_equal", "value": 7}, {"field": "STATUS", "operator": "equal", "value": "ERROR"}], "fields": [{"name": "REQUEST_ID", "dataType": "number"}, {"name": "STATUS"}, {"name": "RUN_DAY"}]}]}, "pitfalls": ["Each chart binds EXACTLY ONE measure; multi-series = one measure + seriesField (see lp:anti:multi-measure-xpt-chart).", "Filter tables with filters:[{field,operator,value}], never an XPath predicate (see lp:anti:xpath-predicate-on-xpt-datatable); a chart's aggregation MAY use the XPath filter knob.", "Headline numbers are kpis cards, not a one-row table (see lp:anti:headline-kpis-as-table).", "No local render for .xpt - verify on the pod (prepareReportForPod -> runReport)."], "sourceRefs": {"atlas": "dashboard-grid-composition #60 + charts #17 + declarative-filters #15; Oracle ESS dashboard exemplar", "verifiedIn": "buildLayoutXpt compile + content checks 2026-08-06 (shard-arch extra harness)"}, "verified": "builder"}
{"id": "lp:archetype:print-dashboard", "kind": "archetype", "name": "Print dashboard (RTF grid: chart | chart + full-width table)", "format": "rtf", "dslSupport": "supported", "description": "The print/PDF dashboard: a heading, a two-column RTF grid with a real vector chart in each cell (chart|chart), and a full-width bordered detail table below. Fixed geometry, BI Beans charts, clean one-page PDF.", "whenToUse": "Print-first or emailed-PDF dashboard. An RTF grid cell CANNOT host a repeating table - side-by-side is chart|chart or chart|KPIs only; 'chart left, repeating detail right' is XPT-only (lp:technique:xpt-grid-chart-table). Full-width detail tables stack BELOW the grid.", "intents": ["pdf dashboard with two charts side by side and a table below", "printable summary page: pie and bar next to each other then the detail list", "email a one page report with charts on top", "fixed layout dashboard for printing"], "requires": {"dataShape": "one detail-grain group (G_ROW) with the category fields (STATUS, REGION) and the measure (AMOUNT); the charts aggregate it per category via for-each-group", "preAggregation": "none needed for the charts (they sum/count per category); pre-aggregate in SQL only if the detail table itself should show summary rows"}, "composition": {"contains": ["lp:technique:rtf-chart"], "notes": "grid columns are TWIPS summing to the printable width (portrait default: 12240 - 2*1440 = 9360). Landscape: page.widthTwips 15840 x heightTwips 12240 and rescale columns (e.g. 6480+6480 at margin 1440)"}, "recipe": {"format": "rtf", "blocks": [{"kind": "heading", "level": 1, "runs": ["Operations Summary"]}, {"kind": "grid", "columns": [4680, 4680], "rows": [{"cells": [{"align": "center", "blocks": [{"kind": "chart", "graphType": "PIE", "title": "Amount by Status", "group": {"select": "/DATA_DS/G_ROW", "by": "STATUS"}, "measures": [{"label": "Amount", "field": "AMOUNT", "agg": "sum"}], "dataLabels": true, "size": {"widthPx": 300, "heightPx": 230}}]}, {"align": "center", "blocks": [{"kind": "chart", "graphType": "BAR_VERT_CLUST", "title": "Amount by Region", "group": {"select": "/DATA_DS/G_ROW", "by": "REGION"}, "measures": [{"label": "Amount", "field": "AMOUNT", "agg": "sum"}], "size": {"widthPx": 300, "heightPx": 230}}]}]}]}, {"kind": "table", "forEach": "/DATA_DS/G_ROW", "sort": [{"expr": "AMOUNT", "order": "descending", "dataType": "number"}], "border": true, "headerBg": "#cfe0f1", "columns": [{"header": "Item", "field": "ITEM_NAME"}, {"header": "Region", "field": "REGION"}, {"header": "Status", "field": "STATUS"}, {"header": "Amount", "field": "AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}]}]}, "fixtureRef": "fixtures/print-dashboard", "pitfalls": ["Grid columns in TWIPS here; the XPT grid uses px (see lp:anti:twips-px-confusion).", "A repeating table INSIDE a grid cell collapses to one garbage row in BIP - keep detail tables full-width below the grid.", "A chart group.select matching 0 nodes renders 'No data to display' at HTTP 200 - check the select against the data XML and LOOK at the chart area after render.", "Charts stay top-level within their cell, never inside a forEach (see lp:anti:chart-in-foreach)."], "sourceRefs": {"atlas": "dashboard-grid-composition #60 (RTF side) + charts #17", "verifiedIn": "shard-arch harness render 2026-08-06: pie + bar side by side, sorted table below"}, "verified": "render"}
{"id": "lp:archetype:pivot-report", "kind": "archetype", "name": "Pivot / matrix report (format-forked: SQL-pivoted flat table for RTF, native Crosstab for XPT)", "format": "both", "dslSupport": "supported", "description": "A matrix ('column per month/SKU/status') delivered per format: RTF = a FLAT table over columns already pivoted in SQL (PIVOT / SUM(CASE)); XPT = the native interactive Crosstab aggregating long detail rows. The template never computes the pivot.", "whenToUse": "Any 'pivot / matrix / column per X' ask. Print -> RTF fork (recipe.blocks): pivot in SQL, template shows a flat table. Interactive -> XPT fork (recipe.xpt): native Crosstab is legitimate (22 real corpus files). NEVER build the pivot with per-cell template sums (lp:anti:template-wide-pivot).", "intents": ["pivot by SKU with one column per month", "matrix of quantity by region and period", "crosstab of counts by status and day", "column per quarter with a total column", "spreadsheet style summary rows by regions columns by months"], "requires": {"dataShape": "RTF fork: one wide row per line (REGION, JAN_QTY, FEB_QTY, ...) with the column set FIXED at design time. XPT fork: LONG detail rows (REGION, MONTH_NAME, QTY) - the Crosstab aggregates them", "preAggregation": "RTF fork: PIVOT or SUM(CASE WHEN ...) columns in SQL, plus a SQL total column when wanted. XPT fork: none - the aggregation lives in the Crosstab"}, "composition": {"notes": "ONE row, TWO recipes (recipe.blocks = rtf, recipe.xpt = crosstab in a grid cell) so the existing lp:anti:template-wide-pivot redirect resolves for both formats. The RTF totals row sums each pivoted COLUMN once - one sum per column is fine; per-CELL cross-group sums are the banned Tama construct"}, "recipe": {"format": "both", "notes": "RTF fork = blocks (flat table over SQL-pivoted columns); XPT fork = xpt (native Crosstab; crosstab components live in grid cells)", "blocks": [{"kind": "heading", "level": 1, "runs": ["Quantity by Region and Month"]}, {"kind": "table", "forEach": "/DATA_DS/G_PIVOT", "border": true, "headerBg": "#cfe0f1", "totalsBg": "#f7cbac", "columns": [{"header": "Region", "field": "REGION"}, {"header": "Jan", "field": "JAN_QTY", "align": "right"}, {"header": "Feb", "field": "FEB_QTY", "align": "right"}, {"header": "Mar", "field": "MAR_QTY", "align": "right"}, {"header": "Total", "field": "TOTAL_QTY", "align": "right"}], "totals": [{"col": 0, "label": "All regions"}, {"col": 1, "expr": "sum(/DATA_DS/G_PIVOT/JAN_QTY)"}, {"col": 2, "expr": "sum(/DATA_DS/G_PIVOT/FEB_QTY)"}, {"col": 3, "expr": "sum(/DATA_DS/G_PIVOT/MAR_QTY)"}, {"col": 4, "expr": "sum(/DATA_DS/G_PIVOT/TOTAL_QTY)"}]}], "xpt": {"label": "Pivot", "format": "xpt", "title": "Quantity by Region and Month", "grid": {"columns": [1554], "rows": [{"cells": [{"crosstab": {"group": "G_DETAIL", "rowDims": ["REGION"], "colDim": "MONTH_NAME", "measure": "QTY", "agg": "summation", "measureLabel": "Qty"}}]}]}}}, "fixtureRef": "fixtures/pivot-report", "pitfalls": ["DYNAMIC column sets (columns discovered at runtime) are NOT supported - BIP's answer is horizontal-break-table, which our generator does not emit yet (lp:cap:horizontal-break-table); fix the column set in SQL at design time.", "XPT Crosstab: filter with filters:[...], not an XPath predicate (lp:anti:xpath-predicate-on-xpt-datatable).", "Never emulate the pivot with per-cell template sums (lp:anti:template-wide-pivot) - it renders but is unmaintainable and banned."], "sourceRefs": {"atlas": "crosstab-pivot #19 (X native 22 files; R emulated only)", "verifiedIn": "rtf fork render-passed via shard-arch harness 2026-08-06; xpt fork buildLayoutXpt compile + content checks (builder level - pod pass pending)"}, "verified": "builder"}
{"id": "lp:archetype:grouped-listing-subtotals", "kind": "archetype", "name": "Grouped listing with per-group subtotals", "format": "rtf", "dslSupport": "supported", "description": "A single-level grouped listing: a section heading per group (department, supplier, category) with its lines table and a per-group SUBTOTAL row; groups flow continuously down the page (no page break per group).", "whenToUse": "'List X grouped by Y with subtotals' where groups share pages. One page per group instead -> splitByPage on the forEach (that is the lp:archetype:statement-run direction). Grand totals belong in SQL (ROLLUP / SUM OVER), shown as a header field or closing paragraph - not template math.", "intents": ["expenses grouped by department with a subtotal per department", "invoice lines by supplier with subtotal rows", "list grouped by category each with its own total", "sales listing per region with section subtotals flowing on the same page"], "requires": {"dataShape": "two-level datamodel output G_GROUP > G_LINE via groupBy; ORDER BY the group column first in the SQL", "preAggregation": "grand totals / percent-of-group as SQL columns (ROLLUP or SUM OVER); the per-group subtotal row itself is template-side relative sum()"}, "composition": {"contains": ["lp:technique:rtf-totals-row"], "notes": "the totals row inside the group forEach scopes automatically to that group's lines (relative sum(G_LINE/...)); deepen to 3 levels via lp:technique:rtf-multilevel-blocks; add splitByPage for page-per-group"}, "recipe": {"format": "rtf", "blocks": [{"kind": "heading", "level": 1, "runs": ["Expenses by Department"]}, {"kind": "forEach", "group": "/DATA_DS/G_DEPT", "blocks": [{"kind": "heading", "level": 2, "runs": [{"field": "DEPT_NAME"}]}, {"kind": "table", "forEach": "G_EXPENSE", "border": true, "headerBg": "#cfe0f1", "totalsBg": "#eef3fb", "columns": [{"header": "Date", "field": "EXPENSE_DATE", "format": {"type": "date", "mask": "dd-MMM-yyyy"}}, {"header": "Description", "field": "EXPENSE_DESC"}, {"header": "Amount", "field": "AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}], "totals": [{"col": 0, "label": "Subtotal", "span": 2}, {"col": 2, "expr": "format-number:sum(G_EXPENSE/AMOUNT);'#,##0.00'"}]}]}]}, "pitfalls": ["Keep the inner group RELATIVE (G_EXPENSE); an absolute path makes every section repeat ALL rows (lp:anti:absolute-inner-group).", "The subtotal expr sum(G_EXPENSE/AMOUNT) is RELATIVE so it scopes per group; an absolute sum() shows the grand total in every section.", "Bind placeholders (G_DEPT, G_EXPENSE) to your output tree."], "sourceRefs": {"atlas": "multi-level-grouping #4 + totals-and-aggregates #16"}, "verified": "builder"}
{"id": "lp:archetype:form-letter", "kind": "archetype", "name": "Form letter / notification (subtemplate chrome + one page per entity)", "format": "rtf", "dslSupport": "supported", "description": "A letter or notification per entity: shared chrome (letterhead header, signature/footer) lives in a catalog .xsb subtemplate imported once and CALLED on each page; the body is merge-field paragraphs; one page per recipient.", "whenToUse": "'Send a letter / notice / notification to each X': dunning letters, confirmations, employee notices. The chrome is reusable across letter types - author it ONCE as a subtemplate (createSubtemplateFile), then import + callTemplate from every letter. Bursting (one PDF per recipient) is a datamodel-side setting on top of this shape.", "intents": ["dunning letter per customer on company letterhead", "employee notification one page each with a shared header and footer", "mail merge style letter with fields in the text", "confirmation notice per supplier using the common template chrome"], "requires": {"dataShape": "one G_RECIPIENT node per letter carrying every merge field; nested groups only if the letter embeds a lines table", "subtemplates": "the .xsb must EXIST in the catalog at the imported path before the letter renders - an unresolvable import kills the whole template"}, "composition": {"notes": "recipe.subtemplate is the companion createSubtemplateFile spec for the chrome; add a lines table inside the forEach for statement-style letters; parameterized chrome calls are not supported yet (lp:cap:parameterized-subtemplate-calls)"}, "recipe": {"format": "rtf", "imports": ["xdoxsl:///Custom/Common Content/Templates/LETTER_CHROME.xsb?loc=en"], "blocks": [{"kind": "forEach", "group": "/DATA_DS/G_RECIPIENT", "splitByPage": true, "blocks": [{"kind": "callTemplate", "name": "LETTER_HEADER"}, {"kind": "paragraph", "runs": ["Dear ", {"field": "RECIPIENT_NAME"}, ","]}, {"kind": "paragraph", "runs": ["Your balance of ", {"field": "BALANCE_DUE", "format": {"type": "currency", "mask": "#,##0.00"}}, " is due on ", {"field": "DUE_DATE", "format": {"type": "date", "mask": "dd-MMM-yyyy"}}, ". Please arrange payment at your earliest convenience."]}, {"kind": "callTemplate", "name": "LETTER_FOOTER"}]}], "subtemplate": {"name": "LETTER_CHROME", "templates": [{"name": "LETTER_HEADER", "blocks": [{"kind": "paragraph", "style": {"bold": true, "size": 14}, "runs": ["ACME CORPORATION"]}, {"kind": "paragraph", "style": {"size": 8}, "runs": ["1 Industrial Way, Springfield"]}]}, {"name": "LETTER_FOOTER", "blocks": [{"kind": "paragraph", "style": {"size": 8}, "runs": ["This is an automated notification. Please do not reply."]}]}]}}, "pitfalls": ["Imports are ALWAYS unconditional at the top of the body; condition the CALL (callTemplate.when), never the import (lp:anti:conditional-import).", "Subtemplate calls take NO parameters (lp:cap:parameterized-subtemplate-calls) - the called template reads fields from the CURRENT data context, so put varying values in the data model.", "Per-letter page-number restart is not supported yet (lp:cap:section-page-restart); splitByPage gives the fresh page only."], "sourceRefs": {"atlas": "subtemplate-libraries #20 (543 importing files) + parameterized-subtemplate-calls #21 gap"}, "verified": "builder"}
{"id": "lp:archetype:simple-tabular", "kind": "archetype", "name": "Simple tabular report with page furniture", "format": "rtf", "dslSupport": "supported", "description": "The bread-and-butter listing: a running page header (title band) and footer repeated on every page, one sorted bordered table over a flat group. No grouping, no charts.", "whenToUse": "Any flat 'list of X' delivered as print/PDF. The running header/footer use the official start:body mechanism and repeat on every page; the table flows across pages automatically. If the user wants interactive sorting/filtering instead, a flat .xpt DataTable is the sibling shape.", "intents": ["simple list of open purchase orders with a page header", "flat report of all transactions with a title on every page", "one table report sorted by number for printing", "basic listing with a footer note on each page"], "requires": {"dataShape": "one flat group (G_ROW) carrying the listing columns; ORDER BY in SQL (or the layout sort spec)"}, "composition": {"notes": "page.header/footer blocks repeat on every page (BIP start:body / end body markers; plain-text tags merge inside them). PAGE x of NUMPAGES page numbering is a BIP capability our generator does not emit yet (atlas page-numbering #25 gap) - say so rather than faking it"}, "recipe": {"format": "rtf", "page": {"marginTwips": 1080, "header": [{"kind": "paragraph", "style": {"bold": true, "align": "center", "size": 14}, "runs": ["Open Purchase Orders"]}], "footer": [{"kind": "paragraph", "style": {"align": "center", "size": 8}, "runs": ["Company Confidential"]}]}, "blocks": [{"kind": "table", "forEach": "/DATA_DS/G_ROW", "sort": [{"expr": "PO_NUMBER", "order": "ascending"}], "border": true, "headerBg": "#cfe0f1", "columns": [{"header": "PO Number", "field": "PO_NUMBER"}, {"header": "Supplier", "field": "SUPPLIER_NAME"}, {"header": "Status", "field": "STATUS"}, {"header": "Amount", "field": "PO_AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}]}]}, "pitfalls": ["Sort syntax is <?sort:EXPR;'order';data-type='number'?> - the SEMICOLON before data-type is required (a comma is rejected by the engine); the builder emits it correctly from the sort spec.", "Page numbering (PAGE of NUMPAGES) and first-page-different headers are BIP capabilities the generator does not emit yet.", "heading/paragraph text is runs[]: literals and {field,format?} objects; there is NO {FIELD} string interpolation."], "sourceRefs": {"atlas": "running-page-headers-footers #23 + sorting #5"}, "verified": "builder"}
{"id": "lp:anti:chart-in-foreach", "kind": "antipattern", "name": "Chart block nested inside a forEach loop", "format": "rtf", "dslSupport": "n/a", "description": "Placing a {kind:'chart'} block among a forEach's child blocks to get 'a chart in each section'.", "whenToUse": "", "intents": ["a chart inside each customer section", "repeat the pie chart per department", "graph within the loop for every group", "per section chart in the printed report", "same chart appears many times down the page"], "trigger": "a chart placed among forEach children, or the ask 'a chart for/under each X'", "why": "The chart shape is re-emitted on EVERY iteration - N copies march down the page (verified duplication), and since the chart's group.select is absolute every copy shows the SAME data. The builder's teaching error spells it out: charts must sit at TOP level.", "instead": ["lp:technique:rtf-chart", "lp:archetype:print-dashboard"], "alternative": "Put ONE chart block at top level before the forEach (chart-on-first-page), or fixed per-category charts side-by-side via the grid (chart|chart). True per-group mini-charts are not supported by the generator.", "verified": "prose"}
{"id": "lp:anti:absolute-inner-group", "kind": "antipattern", "name": "Absolute path on an inner group under an outer forEach", "format": "rtf", "dslSupport": "n/a", "description": "Writing an inner forEach/table group (or the invoice shape's linesGroup) as an absolute /DATA_DS/... path while an outer group wraps it.", "whenToUse": "", "intents": ["inner loop bound to the full data path", "lines group as an absolute path inside the master loop", "every section repeats all rows of the whole report", "totals show the grand total in every group"], "trigger": "an inner forEach/table group (or linesGroup with outerGroup set) starting with '/'", "why": "Every outer iteration walks EVERY node in the document: each master repeats ALL rows of all masters, and absolute sum() totals aggregate across the whole document. buildInvoiceRtf rejects it loudly ('linesGroup ... is ABSOLUTE while outerGroup ... is set - use a path RELATIVE to the outer group'); in raw blocks[] it silently renders a plausible-looking WRONG report.", "instead": ["lp:archetype:statement-run", "lp:technique:rtf-multilevel-blocks", "lp:archetype:invoice-document"], "alternative": "Only the OUTERMOST forEach is absolute (/DATA_DS/G_MASTER); every nested group and every totals sum() path is RELATIVE to its enclosing group (G_LINE, sum(G_LINE/AMOUNT)).", "verified": "prose"}
{"id": "lp:anti:xpt-vocab-in-rtf-chart", "kind": "antipattern", "name": "Wrong chart vocabulary for the format (XPT keys in an RTF chart, or the reverse)", "format": "both", "dslSupport": "n/a", "description": "Writing an RTF chart block with the XPT chart keys (type / dimensionField / string group / measures[].name / agg:'summation'), or an XPT chart with the RTF keys (graphType / group:{select,by} / measures[].field).", "whenToUse": "", "intents": ["rtf chart with dimensionField and type keys", "chart renders a No data to display frame", "reuse the xpt chart json inside the rtf template", "builder rejected my chart keys", "agg summation in an rtf chart measure"], "trigger": "chart keys from the other format's dialect - the two dialects are NOT interchangeable", "why": "Wrong keys used to flow straight into the Graph XML as graphType=\"undefined\" select=\"undefined\" and render a silent 'No data to display' frame at HTTP 200. The RTF builder now auto-translates the unambiguous cases and REJECTS the rest with a per-key fix message (e.g. 'dimensionField is not a field - it belongs in group.by'; 'agg summation - you meant sum').", "instead": ["lp:technique:rtf-chart", "lp:archetype:monitoring-dashboard", "lp:technique:xpt-grid-chart-table"], "alternative": "RTF dialect: graphType + group:{select,by} + measures:[{label,field,agg:sum|count|avg}]. XPT dialect: type + dimensionField + measures:[{name,agg:summation|count|average}] + seriesField. Read the builder's teaching error and swap keys - do not conclude the format 'cannot do charts'.", "verified": "prose"}
{"id": "lp:anti:xpath-predicate-on-xpt-datatable", "kind": "antipattern", "name": "XPath predicate used to filter an XPT DataTable/Crosstab", "format": "xpt", "dslSupport": "n/a", "description": "Filtering an interactive DataTable or Crosstab by baking an XPath predicate into the group path (filter:'DAYS <= 1' or /G_ROW[DAYS<=1]/COL) instead of the real filters block.", "whenToUse": "", "intents": ["xpt detail table shows zero rows after adding a filter", "last 24 hours condition on the data table", "filter the interactive table with an xpath predicate", "table went empty when I added a where condition", "crosstab filtered by a predicate returns nothing"], "trigger": "an .xpt table/crosstab spec using the XPath `filter` knob or a predicated field path", "why": "The interactive processor cannot derive the repeating context from a predicated path - the DataTable renders 0 ROWS (verified). XPath predicates remain valid for RTF for-each and for XPT CHART aggregation; DataTable/Crosstab need the native filters block. Related silent failure: a filter referencing an UNDECLARED field blanks the whole component - the builder auto-declares filter fields in the businessView to prevent this.", "instead": ["lp:archetype:monitoring-dashboard", "lp:technique:xpt-grid-chart-table"], "alternative": "filters:[{field:'DAYS_AGO',operator:'less_or_equal',value:1}] emits the native <filters> block (what Oracle's own ESS dashboard uses). Operators: equal, not_equal, greater, less, greater_or_equal, less_or_equal, top_n, is_null, not_null.", "verified": "prose"}
{"id": "lp:anti:multi-measure-xpt-chart", "kind": "antipattern", "name": "Two or more measures on an interactive XPT chart", "format": "xpt", "dslSupport": "n/a", "description": "Binding 2+ measures[] to one .xpt chart (e.g. NUMBER_OF_SUCCEEDED and NUMBER_OF_ERROR as two series), usually because the data was pre-aggregated into wide columns.", "whenToUse": "", "intents": ["stacked bar with succeeded and error columns as two measures", "two measures on one interactive chart", "chart faults when a second chart is on the page", "multi series chart from wide aggregated columns", "XMLElement getChildNodes param is null"], "trigger": "an .xpt ChartSpec with measures.length > 1, or wide pre-aggregated measure columns feeding a chart", "why": "A multi-measure XPT chart renders when it is ALONE on the page but FAULTS ('XMLElement.getChildNodes() param is null') the moment a second chart shares the page - the builder now rejects >1 measures with the fix in the error. Wide pre-aggregated columns are also the wrong data shape for an interactive chart.", "instead": ["lp:archetype:monitoring-dashboard", "lp:technique:xpt-grid-chart-table"], "alternative": "ONE measure + a series DIMENSION: dimensionField:'RUN_DAY', seriesField:'STATUS', measures:[{name:'REQUEST_ID',agg:'count'}] - multi-series comes from seriesField, and the model feeds long/detail rows, not wide columns. (SCATTER's 2-measure X,Y form is the schema-documented exception.)", "verified": "prose"}
{"id": "lp:anti:twips-px-confusion", "kind": "antipattern", "name": "Grid units swapped between formats (twips vs pixels)", "format": "both", "dslSupport": "n/a", "description": "Copying grid column widths across formats: pixel values (560/1044) into an RTF grid, or twips values (4200/7800) into an XPT LayoutGrid.", "whenToUse": "", "intents": ["dashboard grid renders blank after setting column widths", "column widths 4200 and 7800 in the xpt grid", "grid columns squashed into a narrow sliver", "reuse the rtf grid widths in the interactive layout", "cells pushed off the page"], "trigger": "grid column numbers that are plausible in the OTHER format's unit", "why": "The two grids use different units. XPT LayoutGrid columns are PIXELS (sum ~1554 for a 1650px page): twips-scale values push cells OFF-PAGE and the layout renders BLANK (verified). RTF grid columns are TWIPS (1440/inch, printable ~9360 on the portrait default): px-scale values collapse the grid into a ~1-inch sliver.", "instead": ["lp:archetype:print-dashboard", "lp:technique:xpt-grid-chart-table", "lp:archetype:monitoring-dashboard"], "alternative": "RTF: columns in twips summing to the printable width (pageWidth - 2*margin, default 9360). XPT: columns in px summing to ~pageWidth - 96 (1554 at the 1650px default). RTF column widths on TABLE columns are twips too.", "verified": "prose"}
{"id": "lp:anti:conditional-import", "kind": "antipattern", "name": "Subtemplate import wrapped in a conditional", "format": "rtf", "dslSupport": "n/a", "description": "Putting a <?import:xdoxsl:///...xsb?> inside if/choose ('only import the chrome when X'), or trying to pick imports per condition.", "whenToUse": "", "intents": ["only include the subtemplate when the flag is set", "conditional import of the header xsb", "import inside an if block", "template stopped rendering after adding an import condition", "choose which subtemplate to import at runtime"], "trigger": "an import that the user/agent wants gated by a condition", "why": "BIP resolves imports when the template opens: imports must be UNCONDITIONAL at the very top of the body, and an import inside a conditional (or one that cannot resolve) kills the WHOLE template - nothing renders. The builder's imports[] deliberately cannot express a conditional import; it always emits them unconditionally first.", "instead": ["lp:archetype:form-letter"], "alternative": "Import ALWAYS; make the CALL conditional instead - callTemplate has a when:'cond' knob that wraps only the call (<?if:cond?><?call-template:N?><?end if?>), or use choose/when around alternative callTemplate blocks.", "verified": "prose"}
{"id": "lp:anti:headline-kpis-as-table", "kind": "antipattern", "name": "Headline KPI numbers rendered as a table (or raw-tag TextBoxes)", "format": "xpt", "dslSupport": "n/a", "description": "Modeling headline single-value metrics (total transactions, error count) as a one-row DataTable - or worse, as raw <?xpath?> TextBoxes.", "whenToUse": "", "intents": ["show the totals as a small table at the top", "headline numbers row on the dashboard", "kpi values in a one row grid", "big number cards for total and errors", "put the summary figures in a text box"], "trigger": "single-value metrics heading toward a one-row table or a raw-tag TextBox", "why": "A one-row table reads as data, not as headline cards. The raw-tag TextBox variant is worse: a raw <?xpath?> in a TextBox ERRORS the interactive processor and TRUNCATES the whole layout (verified) - which is exactly why the KPI emitter binds values via DataTableField cards instead.", "instead": ["lp:archetype:monitoring-dashboard"], "alternative": "Use kpis:[{label,field,group}] bound to a single-row KPI group (SQL-aggregated columns) - rendered as caption-over-big-number cards at the top of the dashboard.", "verified": "prose"}
{"id": "lp:cap:section-page-restart", "kind": "technique", "name": "Per-group sections with page-number restart", "format": "rtf", "dslSupport": "unsupported", "description": "BIP can start a new SECTION per group - <?for-each@section:G?> plus <?initial-page-number:?> - so each customer/statement gets its own page numbering (restarting at 1), section-scoped headers, and per-section document properties. 44 corpus templates use @section loops; 450 use initial-page-number. Our generator emits only <?split-by-page-break:?> (fresh page, no numbering restart).", "whenToUse": "Asks like 'page numbers restart for each statement'. Say: supported by BIP, not by our generator yet. Closest supported: splitByPage on the outer forEach (fresh page per group, continuous numbering) - and note page numbering itself is not emitted yet either.", "intents": ["restart page numbers per customer", "each statement numbered page 1 of n separately", "new section per group with its own header", "per invoice page numbering restart"], "pitfalls": ["split-by-page-break gives a new PAGE, not a new SECTION - numbering and running headers continue across groups; do not claim otherwise."], "sourceRefs": {"atlas": "#7 section-page-restart (rtf-only, for-each@section 44 files + initial-page-number 450)"}, "verified": "prose"}
{"id": "lp:cap:multi-copy-printing", "kind": "technique", "name": "Multi-copy printing with per-copy labels", "format": "rtf", "dslSupport": "unsupported", "description": "BIP prints N copies of a document from one data row via a number loop - <?for-each@section:xdoxslt:foreach_number($_XDOCTX,1,$x,1)?> - with per-copy labels ('Original' / 'Duplicate' / 'Customer copy'); 52 corpus templates use it (e.g. Portugal statutory invoices, where the copy count itself comes from data). Our generator has no copy-loop block.", "whenToUse": "'Print three copies, each labeled...' asks. Say: supported by BIP, not by our generator yet. Closest supported: duplicate the rows in SQL (cross join a copy_index row set and expose the label as a column) + splitByPage - or run/deliver the report N times.", "intents": ["print original and duplicate copies of each invoice", "three copies per document with a copy label", "statutory second copy marked duplicate", "copy count driven by document type"], "sourceRefs": {"atlas": "#8 multi-copy-printing (rtf-only, foreach_number 52 files); PortugalInvoicePrintMaster exemplar"}, "verified": "prose"}
{"id": "lp:cap:horizontal-break-table", "kind": "technique", "name": "Horizontal table pagination (wide-table column overflow)", "format": "rtf", "dslSupport": "unsupported", "description": "<?horizontal-break-table:N?> paginates a TOO-WIDE table horizontally: overflow columns continue on the next page with the first N key columns repeated (23 corpus files, e.g. PurchaseOrderLayout). It is BIP's print answer to wide or DYNAMIC pivot column sets. Our generator does not emit it.", "whenToUse": "A pivot/wide listing whose columns cannot fit the page, or a runtime-discovered pivot column set. Say: supported by BIP, not by our generator yet. Closest supported: fix the column set in SQL at design time and make it fit (landscape page + narrower columns via lp:archetype:pivot-report), or use the interactive XPT Crosstab.", "intents": ["pivot with too many columns to fit the page", "table columns continue on the next page", "wide matrix report overflowing horizontally", "a column per week for the whole year in print"], "pitfalls": ["This gap is also why dynamic pivot column sets are out of scope: the RTF fork of lp:archetype:pivot-report requires design-time-known columns."], "sourceRefs": {"atlas": "#33 horizontal-table-pagination (rtf-only, 23 files)"}, "verified": "prose"}
{"id": "lp:cap:etext-family", "kind": "technique", "name": "eText flat-file templates (EFT / positional / delimited interfaces)", "format": "any", "dslSupport": "unsupported", "description": "eText templates produce fixed-position or delimited FLAT FILES (EFT/NACHA bank payments, positional statutory interfaces): <TEMPLATE TYPE> FIXED_POSITION/DELIMITER tables inside an RTF container - 410 corpus files, 429 wired template instances. Our generator has no eText path at all, and its report wiring cannot emit type=etext templates.", "whenToUse": "Bank-file / positional-interface asks. Say: supported by BIP, not by our generator yet. Closest supported: a data-only report (empty templates block) runs the model and returns raw XML for downstream processing; a true eText template must be hand-authored and uploaded/wired manually.", "intents": ["nacha payment file from the payment run", "fixed width bank interface file", "delimited flat file for the clearing house", "positional etext output for payments"], "sourceRefs": {"atlas": "#37 etext-flat-file (410 rtf-container files + 429 etext instances); generator gap #1"}, "verified": "prose"}
{"id": "lp:cap:pdf-acroform-family", "kind": "technique", "name": "PDF AcroForm overlay templates (official-form fill)", "format": "any", "dslSupport": "unsupported", "description": "PDF-form templates overlay data onto a real PDF (classic AcroForm, no XFA): field /T name = XML element, <?rep_field?> repeat markup with page_break, checkbox AP-state matching, same-name value mirroring, split per-box value fields - 433 corpus files (408 are W-4 withholding certificates). This is how pixel-faithful government forms ship. No pdfBuild path exists in our generator.", "whenToUse": "'Fill the official form' / pixel-faithful statutory-form asks. Say: supported by BIP, not by our generator yet. Closest supported: an RTF fixed layout APPROXIMATES a form (labels + bordered grid) but is not the agency's artwork; an existing hand-built PDF template can be uploaded and wired manually.", "intents": ["fill the official W-4 pdf form", "overlay data on the government form", "acroform template with checkboxes", "pixel perfect statutory form output"], "sourceRefs": {"atlas": "#47-#52 pdf-only patterns; generator gap #2 (433 files)"}, "verified": "prose"}
{"id": "lp:cap:excel-template-family", "kind": "technique", "name": "Native Excel XDO_ templates (sheet-per-group workbooks)", "format": "any", "dslSupport": "unsupported", "description": "Native Excel templates drive real .xls workbooks via XDO_ defined names + a hidden XDO_METADATA sheet: XDO_GROUP_ repeats, XDO_SHEET_ sheet-per-group with dynamic tab names, XDO_SKIPROW_ row suppression, XDO_STYLE_ typed/styled cells, even VBA dual-use FBDI re-import workbooks (115 corpus files). Our generator cannot author any of it.", "whenToUse": "'Excel template / one sheet per department / FBDI re-import workbook' asks. Say: supported by BIP, not by our generator yet. Closest supported: BOTH generated layout kinds already EXPORT xlsx (outputFormat includes xlsx) - render an RTF or XPT layout to xlsx for plain tabular Excel output; sheet-per-group and typed-cell control need a hand-built .xls template.", "intents": ["excel template with one sheet per person", "xdo excel workbook with skip rows", "native xls output with typed date cells", "fbdi reimport spreadsheet with data mappings"], "sourceRefs": {"atlas": "#44-#46 excel-only patterns; generator gap #4 (115 files)"}, "verified": "prose"}
{"id": "lp:cap:parameterized-subtemplate-calls", "kind": "technique", "name": "Parameterized subtemplate calls (param@begin / with-param)", "format": "rtf", "dslSupport": "unsupported", "description": "BIP subtemplate calls can take parameters: <?param@begin:?> declarations in the subtemplate plus <?call@inlines:?> / with-param at the call site (370 corpus files carry param@begin, 251+ use call@inlines) - the mechanism behind the entire Fusion Redwood notification chrome (FusionHeaderLogo, FusionTableFooter, ...). Our callTemplate takes NO parameters.", "whenToUse": "Reusable chrome that must vary per call ('the header subtemplate takes the title / logo url as a parameter'). Say: supported by BIP, not by our generator yet. Closest supported: parameterless callTemplate - the called template reads fields from the CURRENT data context, so put the varying values in the data model (lp:archetype:form-letter).", "intents": ["pass a parameter to the subtemplate", "call template with param like the redwood notifications", "reusable header that takes a title argument", "with-param in the xsb call"], "sourceRefs": {"atlas": "#21 parameterized-subtemplate-calls (rtf-only, 370 files); generator gap #9"}, "verified": "prose"}
{"id": "lp:cap:conditional-formatting-rules", "kind": "technique", "name": "Data-driven conditional formatting (highlight rules)", "format": "both", "dslSupport": "unsupported", "description": "Data-driven styling: RTF <?attribute@incontext:?> flips background/border/bold per row value (42 files); XPT carries real conditionalFormatting conditions including FIELD-VS-FIELD comparisons (27 real files - e.g. highlight when actual rating < target rating). Our generator emits only static styles and an empty <conditionalFormatting/> placeholder.", "whenToUse": "'Highlight overdue rows in red' asks. Say: supported by BIP, not by our generator yet. Closest supported: compute a status/severity COLUMN in SQL and show it (sortable/filterable, honest data), or split rows into separate filtered tables (an 'Errors' table with statically shaded columns via bg/headerBg).", "intents": ["highlight rows in red when overdue", "conditional cell background by value", "bold the amount when it exceeds the limit", "color status cells by severity", "highlight when actual is below target"], "sourceRefs": {"atlas": "#14 conditional-formatting (R attribute@incontext 42 + X conditions 27); generator gap #10"}, "verified": "prose"}
{"id": "lp:cap:dynamic-watermark", "kind": "technique", "name": "Dynamic (data-bound) watermark", "format": "rtf", "dslSupport": "unsupported", "description": "A WordArt watermark whose TEXT is bound to data (gtext shape + <?WATERMARK_FIELD?>, the PowerPlusWaterMarkObject pattern, 23 corpus files) - e.g. DRAFT / COPY / document status stamped diagonally across every page from a data field. Not emitted by our generator.", "whenToUse": "'Stamp DRAFT across the page' / status-watermark asks. Say: supported by BIP, not by our generator yet. Closest supported: a static heading/paragraph banner or a static background image block; true per-data diagonal watermark text needs a hand-authored template.", "intents": ["draft watermark across every page", "stamp the document status diagonally", "watermark text from a data field", "paid stamp on the invoice pages"], "sourceRefs": {"atlas": "#30 dynamic-watermark (rtf-only, 23 files)"}, "verified": "prose"}
{"id": "lp:technique:rtf-page-header-footer", "kind": "technique", "name": "Running page header/footer (start:body markers)", "format": "rtf", "dslSupport": "supported", "description": "A page header and footer that repeat on EVERY printed page, declared as block trees under page.header / page.footer. Emitted via the official BIP body markers: header blocks before <?start:body?>, footer blocks after <?end body?>.", "whenToUse": "Any print report needing page furniture - report/company name on top, confidentiality or legal line at the bottom - independent of where the body paginates. Fields (with format masks) work inside the bands.", "intents": ["repeat the company name at the top of every page", "put a confidential notice in the page footer", "running header with the report title and run date on all pages", "footer on every page of the pdf"], "requires": {"dataShape": "any; header/footer fields resolve from the ROOT context (root-level elements like REPORT_TITLE, RUN_DATE)"}, "composition": {"nestsIn": [], "contains": [], "notes": "page.header/page.footer sit BESIDE blocks on the rtf layout spec (inside the page object), not inside blocks[]. Combine with any body. For 'Page x of y' numbers in the footer see lp:technique:rtf-raw-page-x-of-y."}, "recipe": {"page": {"header": [{"kind": "paragraph", "style": {"bold": true}, "runs": [{"field": "REPORT_TITLE"}, " - run date: ", {"field": "RUN_DATE", "format": {"type": "date", "mask": "DD-MON-YYYY"}}]}], "footer": [{"kind": "paragraph", "style": {"align": "center", "size": 9}, "runs": ["Company Confidential"]}]}, "blocks": [{"kind": "heading", "level": 1, "runs": ["Transaction Listing"]}, {"kind": "table", "forEach": "/DATA_DS/G_LINE", "border": true, "headerBg": "#cfe0f1", "columns": [{"header": "Line", "field": "LINE_NUMBER", "align": "right", "width": 1200}, {"header": "Description", "field": "LINE_DESCRIPTION", "width": 5160}, {"header": "Amount", "field": "LINE_AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right", "width": 3000}]}]}, "fixtureRef": "fixtures/page-hf", "pitfalls": ["Do NOT hand-build raw \\sectd or {\\header...} RTF groups - they make BIP tags STOP MERGING (literal/empty output). page.header/page.footer is the supported mechanism.", "page.header/page.footer take BLOCK trees (same shapes as blocks[]), not strings.", "Everything in page.header repeats on every page - one-time cover content belongs in blocks[], not the header."], "sourceRefs": {"atlas": "running-page-headers-footers (#23; start:body/end body in 44 real files)", "verifiedIn": "shard harness PASS (body) + verify-extras.mts full-spec render 2026-08-06: header+footer repeat on pages 1 and 2 (PNG check)"}, "verified": "render"}
{"id": "lp:technique:rtf-format-masks", "kind": "technique", "name": "Format masks: date/number/currency on field AND expr columns", "format": "rtf", "dslSupport": "supported", "description": "Engine-validated format masks - <?format-date/number/currency:VALUE;'MASK'?> - applied via format:{type,mask} on table columns (plain field columns AND computed expr columns) and on {field,format} runs in paragraphs/headings.", "whenToUse": "Whenever raw data values (ISO dates, unformatted numbers) must print as business values. Apply the mask at EVERY point a value is shown - expr columns need it exactly like field columns.", "intents": ["format the dates as 15-Jan-2026 in the table", "show amounts with thousands separators and two decimals", "currency format on the amount due column", "the computed column prints raw numbers - format it too"], "requires": {"dataShape": "dates as ISO yyyy-MM-dd (xsd:date) in the output XML; numbers delivered unformatted by SQL"}, "composition": {"nestsIn": ["lp:technique:rtf-invoice-shape", "lp:technique:rtf-multilevel-blocks"], "notes": "omitting mask falls back to engine defaults: XDODEFDATE / #,##0.00 / XDODEFCC"}, "recipe": {"blocks": [{"kind": "paragraph", "runs": [{"text": "Run date: ", "bold": true}, {"field": "RUN_DATE", "format": {"type": "date", "mask": "DD-MON-YYYY"}}]}, {"kind": "table", "forEach": "/DATA_DS/G_LINE", "border": true, "columns": [{"header": "Invoice Date", "field": "INVOICE_DATE", "format": {"type": "date", "mask": "dd-MMM-yyyy"}}, {"header": "Qty", "field": "QTY", "align": "right"}, {"header": "Unit Price", "field": "UNIT_PRICE", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}, {"header": "Extended", "expr": "QTY * UNIT_PRICE", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}, {"header": "Amount Due", "field": "AMOUNT_DUE", "format": {"type": "currency", "mask": "#,##0.00"}, "align": "right"}]}]}, "fixtureRef": "fixtures/format-masks", "pitfalls": ["An expr column is STILL a value to mask - {expr:'QTY * UNIT_PRICE'} without format ships raw numbers (verified past failure: raw ISO timestamps in rendered tables).", "Two mask families are accepted - Oracle-style 'DD-MON-YYYY' and Java-style 'dd-MMM-yyyy' - pick ONE per mask, never blend them. Render-observed: 'DD-MON-YYYY' prints 06-Aug-2026 (the engine title-cases the month, it does NOT print AUG).", "Masks format at display time only - the SQL still delivers clean typed values; do not TO_CHAR in SQL and mask again."], "sourceRefs": {"atlas": "format-masks-localized (#26; format-date/number/currency in 859 real files)", "verifiedIn": "shard harness render PASS + PNG check 2026-08-06: both mask families + masked expr column verified visually"}, "verified": "render"}
{"id": "lp:technique:rtf-sort", "kind": "technique", "name": "Sort within a repeating region (table forEach / forEach block)", "format": "rtf", "dslSupport": "supported", "description": "Engine-validated <?sort:EXPR;'order';data-type='number'?> tags emitted right after the for-each opens, via sort:[{expr,order,dataType}] on a table (with forEach) or on a forEach block.", "whenToUse": "Order rows in the layout when the SQL order cannot serve - typically one dataset feeding SEVERAL differently-ordered sections. When only one order is needed, prefer ORDER BY in SQL.", "intents": ["sort the lines by amount descending", "order the table by vendor name", "largest amounts first in the report", "alphabetical vendor list from the same data"], "composition": {"nestsIn": ["lp:technique:rtf-invoice-shape", "lp:technique:rtf-multilevel-blocks"], "notes": "sort is a property OF the repeating spec (table.sort / forEach.sort) - there is no standalone sort block; multiple keys = multiple array entries in priority order"}, "recipe": {"blocks": [{"kind": "heading", "level": 2, "runs": ["Lines by amount (descending)"]}, {"kind": "table", "forEach": "/DATA_DS/G_LINE", "sort": [{"expr": "LINE_AMOUNT", "order": "descending", "dataType": "number"}], "border": true, "columns": [{"header": "Vendor", "field": "VENDOR_NAME"}, {"header": "Amount", "field": "LINE_AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}]}, {"kind": "heading", "level": 2, "runs": ["Vendors (alphabetical, same data)"]}, {"kind": "forEach", "group": "/DATA_DS/G_LINE", "sort": [{"expr": "VENDOR_NAME", "order": "ascending"}], "blocks": [{"kind": "paragraph", "runs": [{"field": "VENDOR_NAME"}]}]}]}, "fixtureRef": "fixtures/sort-desc", "pitfalls": ["The separator before data-type is a SEMICOLON - <?sort:AMOUNT;'descending';data-type='number'?>; a comma is rejected by the engine ('Incorrect XPath').", "Numeric sorts NEED dataType:'number' - a text sort puts 980.00 above 15,000.00.", "Sort keys are evaluated in the group's row context - use line-level element names."], "sourceRefs": {"atlas": "sorting (#5)", "verifiedIn": "shard harness render PASS + PNG check 2026-08-06: descending numeric table + alphabetical forEach list from deliberately shuffled fixture"}, "verified": "render"}
{"id": "lp:technique:rtf-conditional-if", "kind": "technique", "name": "Conditional content: if / choose / context conditionals (if@CTX)", "format": "rtf", "dslSupport": "supported", "description": "if{test,blocks} emits <?if:test?>...<?end if?>; choose{whens[{test,blocks}],otherwise} emits <?choose:?>/<?when?>/<?otherwise?>; adding context:'inlines'|'row'|'cell'|'column'|'section' emits <?if@CTX:test?> to scope exactly what gets hidden (the close stays plain <?end if?>).", "whenToUse": "Show/hide presentation by data: warning paragraphs for overdue rows, value-band texts, inline badges, whole-section gates. For hiding a TABLE COLUMN use the per-column guard instead (lp:technique:rtf-column-guard).", "intents": ["show a warning only when the document is overdue", "different text depending on the amount band", "display ON HOLD next to held documents only", "hide this section when the flag is N"], "composition": {"nestsIn": ["lp:technique:rtf-multilevel-blocks", "lp:technique:rtf-invoice-shape"], "notes": "if/choose blocks nest freely inside forEach; tests are XPath in the enclosing context"}, "recipe": {"blocks": [{"kind": "forEach", "group": "/DATA_DS/G_ROW", "blocks": [{"kind": "heading", "level": 3, "runs": [{"field": "DOC_NUMBER"}, " - ", {"field": "STATUS"}]}, {"kind": "if", "test": "STATUS='OVERDUE'", "blocks": [{"kind": "paragraph", "style": {"bold": true}, "runs": ["ACTION REQUIRED: this document is overdue."]}]}, {"kind": "choose", "whens": [{"test": "AMOUNT > 1000", "blocks": [{"kind": "paragraph", "runs": ["High value: ", {"field": "AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}}]}]}, {"test": "AMOUNT > 100", "blocks": [{"kind": "paragraph", "runs": ["Mid value."]}]}], "otherwise": [{"kind": "paragraph", "runs": ["Low value."]}]}, {"kind": "if", "context": "inlines", "test": "HOLD_FLAG='Y'", "blocks": [{"kind": "paragraph", "runs": ["ON HOLD"]}]}]}]}, "fixtureRef": "fixtures/conditional", "pitfalls": ["Tests are relative to the ENCLOSING for-each context - STATUS='OVERDUE' inside forEach G_ROW reads the current row's element.", "The close of a context conditional is plain <?end if?> - never <?end if@row?>.", "A conditional with an SQL equivalent (CASE WHEN) belongs in SQL - keep template conditionals for presentation, not computation (simplicity contract)."], "sourceRefs": {"atlas": "conditional-display (#12; if/if@inlines/choose in 657 real files)", "verifiedIn": "shard harness render PASS + PNG check 2026-08-06: per-row branches match fixture statuses exactly"}, "verified": "render"}
{"id": "lp:technique:rtf-grid-chart-chart", "kind": "technique", "name": "RTF dashboard grid: two charts side-by-side", "format": "rtf", "dslSupport": "supported", "description": "A grid block (borderless RTF table; column widths in TWIPS) with one row of two cells, each hosting a chart block - the print answer to a 2-up dashboard band.", "whenToUse": "Print/PDF dashboards wanting two charts on one band (pie + bar, trend + breakdown). If the ask is chart BESIDE A REPEATING DETAIL TABLE, that layout is XPT-only - an RTF grid cell cannot host a repeating table.", "intents": ["pie chart and bar chart side by side on the pdf", "two charts in one row of the print dashboard", "chart left chart right layout in rtf"], "requires": {"dataShape": "one detail-grain group; each chart aggregates it per its own group.by"}, "composition": {"contains": ["lp:technique:rtf-chart"], "conflicts": ["repeating detail table inside an RTF grid cell (that shape is xpt-only: lp:technique:xpt-grid-chart-table)"], "notes": "cells can also hold heading/paragraph/image; a table in a cell renders as a COMPACT non-repeating panel only"}, "recipe": {"blocks": [{"kind": "heading", "level": 1, "runs": ["Spend Overview"]}, {"kind": "grid", "columns": [4680, 4680], "rows": [{"cells": [{"align": "center", "blocks": [{"kind": "chart", "graphType": "PIE", "title": "Amount by Category", "group": {"select": "/DATA_DS/G_ROW", "by": "CATEGORY_NAME"}, "measures": [{"label": "Amount", "field": "AMOUNT", "agg": "sum"}], "dataLabels": true, "size": {"widthPx": 300, "heightPx": 225}}]}, {"align": "center", "blocks": [{"kind": "chart", "graphType": "BAR_VERT_CLUST", "title": "Orders by Category", "group": {"select": "/DATA_DS/G_ROW", "by": "CATEGORY_NAME"}, "measures": [{"label": "Orders", "field": "ORDER_COUNT", "agg": "sum"}], "size": {"widthPx": 300, "heightPx": 225}}]}]}]}]}, "fixtureRef": "fixtures/grid-2charts", "pitfalls": ["RTF grid columns are TWIPS (1440/inch; default printable width 9360) - pixel values like 560/1044 produce micro-cells. The PIXEL grid is the XPT one.", "Size each chart to FIT its cell: chart widthPx*15 twips must be <= its column width (300px = 4500 twips fits a 4680 column).", "A chart's group.select stays ABSOLUTE (/DATA_DS/G_ROW) - a grid cell does not open a for-each context.", "A select matching 0 nodes renders a normal-looking frame with the literal 'No data to display' - LOOK at the chart areas after render."], "sourceRefs": {"atlas": "dashboard-grid-composition (#60) + charts (#17)", "verifiedIn": "shard harness render PASS + PNG check 2026-08-06: pie and bar side by side, correct aggregation, no 'No data to display'"}, "verified": "render"}
{"id": "lp:technique:rtf-grid-chart-kpis", "kind": "technique", "name": "RTF dashboard grid: chart | KPI text panel", "format": "rtf", "dslSupport": "supported", "description": "A grid row pairing a chart cell with a shaded KPI panel: headings + big bold paragraphs bound to a single-row SQL totals group. The print replacement for the XPT kpis row.", "whenToUse": "Print dashboard band with one chart plus 2-4 headline numbers. KPI values come from a single-row totals group computed in SQL - never template math.", "intents": ["chart with the key totals next to it", "headline numbers beside the graph on the pdf", "kpi cards and a chart in one row for print"], "requires": {"dataShape": "detail group for the chart + a SINGLE-ROW totals group (e.g. G_TOTALS with SQL SUM/COUNT columns) for the KPI panel", "preAggregation": "KPI values as SQL aggregate columns (SUM OVER / COUNT), not sum() template expressions"}, "composition": {"contains": ["lp:technique:rtf-chart"], "conflicts": ["headline KPIs rendered as a table (banned - they are big bold paragraphs in a shaded cell)"], "notes": "swap cells or add rows for more bands; combine with lp:technique:rtf-landscape for wide dashboards"}, "recipe": {"blocks": [{"kind": "heading", "level": 1, "runs": ["Receivables Snapshot"]}, {"kind": "grid", "columns": [5400, 3960], "rows": [{"cells": [{"align": "center", "blocks": [{"kind": "chart", "graphType": "BAR_HORIZ_CLUST", "title": "Amount by Status", "group": {"select": "/DATA_DS/G_ROW", "by": "STATUS"}, "measures": [{"label": "Amount", "field": "AMOUNT", "agg": "sum"}], "size": {"widthPx": 320, "heightPx": 240}}]}, {"bg": "#f4f8fd", "blocks": [{"kind": "heading", "level": 3, "runs": ["Total Amount"]}, {"kind": "paragraph", "style": {"bold": true, "size": 24}, "runs": [{"field": "/DATA_DS/G_TOTALS/TOTAL_AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}}]}, {"kind": "heading", "level": 3, "runs": ["Documents"]}, {"kind": "paragraph", "style": {"bold": true, "size": 24}, "runs": [{"field": "/DATA_DS/G_TOTALS/DOC_COUNT"}]}]}]}]}]}, "fixtureRef": "fixtures/grid-chart-kpis", "pitfalls": ["Bind KPI paragraphs by ABSOLUTE path ({field:'/DATA_DS/G_TOTALS/TOTAL_AMOUNT'}) - there is no enclosing forEach inside a grid cell.", "Headline KPIs are NOT a table - big bold paragraphs (style.size) in a shaded cell (bg) read as cards.", "Grid columns in twips; cell bg takes #hex."], "sourceRefs": {"atlas": "dashboard-grid-composition (#60)", "verifiedIn": "shard harness render PASS + PNG check 2026-08-06: bar chart left, shaded panel right with 19,500.00 and 4"}, "verified": "render"}
{"id": "lp:technique:rtf-header-fields-band", "kind": "technique", "name": "Header fields band (label: value pairs above the lines table)", "format": "rtf", "dslSupport": "supported", "description": "The invoice-style band of bold 'Label: value' lines above the line-item table - paragraphs pairing a bold literal run ({text,bold:true}) with a field run. The rtf layout spec's headerFields key generates exactly this band.", "whenToUse": "Document-shaped output (PO, invoice, remittance) where a handful of master fields sit above the lines. For one document per master node, wrap band + table in a forEach (see lp:technique:rtf-invoice-shape).", "intents": ["supplier and po number above the lines table", "invoice header details on top then the items", "label value pairs at the top of the document"], "requires": {"dataShape": "master fields at the ROOT (single document) or on the outer group node (one document per node)"}, "composition": {"nestsIn": ["lp:technique:rtf-invoice-shape"], "notes": "convenience path: zRtfLayout headerFields:[{label,field|expr}] + linesGroup/columns emits this same shape (empty label = value only, no colon); use explicit blocks when the band needs anything beyond plain pairs"}, "recipe": {"blocks": [{"kind": "heading", "level": 1, "runs": ["Purchase Order"]}, {"kind": "paragraph", "runs": [{"text": "Supplier: ", "bold": true}, {"field": "SUPPLIER_NAME"}]}, {"kind": "paragraph", "runs": [{"text": "PO Number: ", "bold": true}, {"field": "PO_NUMBER"}]}, {"kind": "paragraph", "runs": [{"text": "Order Date: ", "bold": true}, {"field": "PO_DATE", "format": {"type": "date", "mask": "dd-MMM-yyyy"}}]}, {"kind": "table", "forEach": "/DATA_DS/G_LINE", "border": true, "headerBg": "#cfe0f1", "columns": [{"header": "Item", "field": "ITEM_NAME"}, {"header": "Qty", "field": "QTY", "align": "right"}, {"header": "Amount", "field": "AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}]}]}, "fixtureRef": "fixtures/header-band", "pitfalls": ["Bold the LABEL run only ({text:'Supplier: ',bold:true}) - a paragraph-level style bolds the value too.", "Empty label means value-only - never emit an orphan ': ' prefix.", "Inside an outer forEach the band fields go RELATIVE to the group node (no leading /)."], "sourceRefs": {"atlas": "field-binding (#1) as the invoice header band", "verifiedIn": "shard harness render PASS + PNG check 2026-08-06: three bold-label pairs above the bordered lines table"}, "verified": "render"}
{"id": "lp:technique:rtf-static-image", "kind": "technique", "name": "Static embedded PNG image (logo/banner)", "format": "rtf", "dslSupport": "supported", "description": "An image block embeds literal PNG bytes (base64) into the RTF as \\pict\\pngblip with optional widthTwips/heightTwips scaling and alignment. Static bytes only - the image travels inside the template.", "whenToUse": "Fixed chrome: company logo, banner, signature block. ASK the user to provide the real PNG bytes; use a neutral placeholder until they do.", "intents": ["put our logo at the top of the report", "company banner image on the letterhead", "embed a static image in the template"], "composition": {"nestsIn": ["lp:technique:rtf-page-header-footer"], "notes": "an image block also works inside page.header (logo on every page) and inside a grid cell"}, "recipe": {"blocks": [{"kind": "image", "pngBase64": "iVBORw0KGgoAAAANSUhEUgAAAHgAAAAoCAIAAAC6iKlyAAAAUklEQVR42u3QQQ0AAAgEoGtkNKNY2RbOBxsJSPVwIApEi0a0aNEWRItGtGjRFkSLRrRo0YgWjWjRohEtGtGiRSNaNKJFi0a0aESLFo1o0Yj+ZwEDlw0LC9YMWQAAAABJRU5ErkJggg==", "widthTwips": 2400, "heightTwips": 800, "align": "left"}, {"kind": "heading", "level": 1, "runs": ["Statement of Account"]}, {"kind": "paragraph", "runs": ["Prepared for ", {"field": "ACCOUNT_NAME"}, ". Replace the placeholder PNG above with the customer's logo bytes."]}]}, "fixtureRef": "fixtures/static-image", "pitfalls": ["Dynamic per-row images (url:{...} alt-text binding, atlas #28) are NOT supported by the generator - static bytes only; say so instead of faking it.", "NEVER fabricate a real-looking logo - ship the neutral placeholder and ask the user for their PNG.", "widthTwips/heightTwips scale the render (1440 twips = 1 inch); omitted, the PNG renders at intrinsic px @96dpi.", "PNG only (\\pngblip) - convert JPEG/GIF before embedding."], "sourceRefs": {"atlas": "static-images (#29)", "verifiedIn": "shard harness render PASS + PNG check 2026-08-06: slate-blue placeholder rectangle above the heading"}, "verified": "render"}
{"id": "lp:technique:rtf-landscape", "kind": "technique", "name": "Landscape page geometry (widthTwips > heightTwips)", "format": "rtf", "dslSupport": "supported", "description": "Landscape print by page geometry alone: page:{widthTwips:15840, heightTwips:12240} (11in x 8.5in) - the correct DSL surface. Builder emits the geometry after the font/color tables (the only placement the BIP engine honors); render-verified true 792x612 landscape output.", "whenToUse": "Wide tables (7+ columns), two-chart dashboard bands, anything that clips or wraps in portrait. Shrink marginTwips (720 = 0.5in) to buy more printable width.", "intents": ["make the report landscape", "the table is too wide for portrait", "rotate the page so all columns fit"], "composition": {"notes": "combines with any body; plan table column widths against the new printable width (pageWidth - 2*margin)"}, "recipe": {"page": {"widthTwips": 15840, "heightTwips": 12240, "marginTwips": 720}, "blocks": [{"kind": "heading", "level": 1, "runs": ["Open Documents (Landscape)"]}, {"kind": "table", "forEach": "/DATA_DS/G_LINE", "border": true, "headerBg": "#cfe0f1", "columns": [{"header": "Document", "field": "DOC_NUMBER", "width": 1800}, {"header": "Supplier", "field": "SUPPLIER_NAME", "width": 2600}, {"header": "Site", "field": "SITE_NAME", "width": 1700}, {"header": "Terms", "field": "TERMS_NAME", "width": 1500}, {"header": "Date", "field": "DOC_DATE", "format": {"type": "date", "mask": "dd-MMM-yyyy"}, "width": 1800}, {"header": "Status", "field": "STATUS", "width": 1400}, {"header": "Curr", "field": "CURRENCY_CODE", "width": 1200}, {"header": "Amount", "field": "AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right", "width": 2400}]}]}, "fixtureRef": "fixtures/landscape", "pitfalls": ["Page geometry control words (\\paperw/\\paperh/\\marg*) are honored by the BIP engine ONLY when they directly follow an {\\info} group placed after {\fonttbl}/{\\colortbl} (the builder emits this since 2026-08-06, even when no title is set). In any other position - before the font table, without {\\info}, in the body, or via \\sectd/\\pgwsxn - they are silently ignored and output stays letter portrait.", "Landscape is nothing but width > height - there is no 'orientation' key.", "Re-plan column widths for the landscape printable width (15840 - 2*720 = 14400 twips) - portrait widths leave a third of the page blank.", "Mixed portrait+landscape sections in ONE template are not supported by the generator (corpus does it with \\sect breaks; atlas #59 partial)."], "sourceRefs": {"atlas": "page-geometry (#59; \\lndscpsxn in 1,386 real files - ours via paper size)", "verifiedIn": "shard harness render PASS (content only - page stayed portrait, PNG-measured 612x792) + land-exp.mts placement experiment 2026-08-06: pageCtl moved after fonttbl renders MediaBox 792x612 with the table+fields intact; recipe kept as the correct DSL surface pending the builder fix"}, "verified": "render"}
{"id": "lp:technique:rtf-column-guard", "kind": "technique", "name": "Column guard: show/hide a whole table column by a flag", "format": "rtf", "dslSupport": "supported", "description": "Per-column guard:{context:'column'|'cell', test} wraps that column's header AND body cells with <?if@column:test?>...<?end if?> opened and closed CONTIGUOUSLY inside each cell, so the column shows or hides as a unit.", "whenToUse": "Parameter- or data-driven column visibility: cost columns only for buyers, ratings only when a PSHOW flag is Y. Corpus-verbatim shape: <?if@column://PSHOWWORKERRATINGSFLAG='Y'?>.", "intents": ["hide the cost column unless the flag is Y", "only show ratings when the parameter allows it", "optional column controlled by a report parameter"], "requires": {"dataShape": "the flag as a root-level element (reference it as //FLAG or an absolute path so it resolves from inside the row context)"}, "composition": {"nestsIn": ["lp:technique:rtf-invoice-shape", "lp:technique:rtf-multilevel-blocks"], "notes": "guard is a COLUMN property; the if-block form with context:'column' exists but the per-column guard is the right tool for tables (wraps header+body contiguously)"}, "recipe": {"tableFragment": {"kind": "table", "forEach": "/DATA_DS/G_LINE", "border": true, "headerBg": "#cfe0f1", "columns": [{"header": "Item", "field": "ITEM_NAME"}, {"header": "Qty", "field": "QTY", "align": "right"}, {"header": "Cost", "field": "UNIT_COST", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right", "guard": {"context": "column", "test": "//PSHOW_COST_FLAG='Y'"}}]}}, "fixtureRef": "fixtures/column-guard", "pitfalls": ["The guard lives ON the column definition - do not wrap the whole table in an if block with context:'column'; the conditional must open and close INSIDE each cell, never span the row.", "Use //FLAG (or an absolute path) in the test - a bare FLAG resolves against the ROW group and silently never matches.", "context 'cell' hides only the body cell content; 'column' is the whole-column corpus shape (header included)."], "sourceRefs": {"atlas": "conditional-region-removal (#13; if@column/if@cell in 46 real files)", "verifiedIn": "shard harness render PASS (flag N: Cost column content hidden) + verify-extras.mts flag-Y render shows it; PNG-checked both 2026-08-06"}, "verified": "render"}
{"id": "lp:technique:rtf-subtemplate-call", "kind": "technique", "name": "Subtemplate import + callTemplate (unconditional import, optionally wrapped call)", "format": "rtf", "dslSupport": "supported", "description": "imports:[...] emits <?import:xdoxsl:///<catalog path>.xsb?loc=en?> UNCONDITIONALLY at the very top of the body; a callTemplate block emits <?call-template:NAME?> where the shared content should appear - optionally condition-wrapped via when.", "whenToUse": "Shared chrome (footers, terms blocks, notification headers) maintained once in a catalog .xsb and reused across templates. Build the .xsb itself with createSubtemplateFile (templates:[{name,blocks}]).", "intents": ["reuse the shared footer subtemplate in this report", "import the common blocks and call the note template", "conditionally call a catalog subtemplate"], "composition": {"contains": [], "notes": "the .xsb defines <?template:NAME?>...<?end template?> entries; ONE import serves any number of callTemplate blocks"}, "recipe": {"imports": ["xdoxsl:///Custom/Common Content/Templates/AI_COMMON.xsb?loc=en"], "blocks": [{"kind": "heading", "level": 1, "runs": ["Remittance Advice"]}, {"kind": "table", "forEach": "/DATA_DS/G_LINE", "border": true, "columns": [{"header": "Invoice", "field": "INVOICE_NUM"}, {"header": "Paid", "field": "PAID_AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}]}, {"kind": "callTemplate", "name": "FOOTER_NOTE", "when": "SHOW_NOTE='Y'"}]}, "fixtureRef": "fixtures/subtemplate-call", "pitfalls": ["Imports must NEVER sit inside if/choose - an import resolves at compile time; a conditional or unresolvable import kills the WHOLE template. Condition the CALL, not the import.", "A call-template name that no import defines is an XSL compile error (template not found) - the whole render fails, not a silent no-op.", "Parameterized calls (param@begin / with-param, atlas #21) are NOT supported by the generator yet - calls are parameterless.", "The when test evaluates at the call site's context (root after top-level blocks)."], "sourceRefs": {"atlas": "subtemplate-libraries (#20; import xdoxsl in 543 real files, call in 628)", "verifiedIn": "verify-extras.mts 2026-08-06: main+.xsb rendered through bip-render's subtemplates[] contract (the same one renderTemplate uses); note appears exactly once under the table (PNG check). The main harness CANNOT exercise this row (it drops imports[] and sends no subtemplates - an unresolved call-template is an XSL static error XML-22049), so the fixture data file is named data.xml to keep the harness at SKIP instead of a misleading FAIL."}, "verified": "render"}
{"id": "lp:technique:rtf-raw-page-x-of-y", "kind": "technique", "name": "Page x of y footer (raw Word PAGE/NUMPAGES fields)", "format": "rtf", "dslSupport": "partial", "description": "'Page 1 of 7' numbering via raw-idiom LEAF runs: the Word field codes for PAGE and NUMPAGES embedded as {raw} runs inside the page.footer paragraph. BIP maps them to per-page numbers at render. No first-class DSL key yet - hence partial.", "whenToUse": "Any multi-page print document that needs page numbering. Keep the raw runs leaf-level (the two field codes only) - never build loops or conditionals in raw.", "intents": ["page 1 of 7 at the bottom of each page", "number the pages in the footer", "show total page count on every page"], "composition": {"nestsIn": ["lp:technique:rtf-page-header-footer"], "notes": "composes with any page.footer content; the surrounding literals ('Page ', ' of ') are normal string runs"}, "recipe": {"page": {"footer": [{"kind": "paragraph", "style": {"align": "center", "size": 9}, "runs": ["Page ", {"raw": "{\\field{\\*\\fldinst  PAGE }{\\fldrslt 1}}"}, " of ", {"raw": "{\\field{\\*\\fldinst  NUMPAGES }{\\fldrslt 1}}"}]}]}, "blocks": [{"kind": "heading", "level": 1, "runs": ["General Ledger Listing"]}, {"kind": "table", "forEach": "/DATA_DS/G_LINE", "border": true, "columns": [{"header": "Entry", "field": "ENTRY_NUMBER", "align": "right"}, {"header": "Account", "field": "ACCOUNT_NAME"}, {"header": "Debit", "field": "DEBIT_AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}]}]}, "fixtureRef": "fixtures/page-x-of-y", "pitfalls": ["raw runs are emitted UNESCAPED - keep them to these two field codes; structural RTF in raw (loops, sectd, header groups) is banned.", "The literal 1 inside {\\fldrslt 1} is only Word's cached preview - BIP replaces it per page at render.", "Per-section page-number RESTART (initial-page-number, atlas #7) is a separate unsupported capability - this idiom numbers the whole document."], "sourceRefs": {"atlas": "page-numbering (#25; PAGE/NUMPAGES fields in 900 real files)", "verifiedIn": "shard harness PASS (body) + verify-extras.mts full-spec render 2026-08-06: PNG check shows 'Page 1 of 2' / 'Page 2 of 2' in the footers"}, "verified": "render"}
{"id": "lp:technique:rtf-invoice-shape", "kind": "technique", "name": "Invoice shape: master forEach + header band + lines table + totals (page per document)", "format": "rtf", "dslSupport": "supported", "description": "The classic two-level document: outer forEach over the master/invoice group (splitByPage = one document per page), header-band paragraphs, then the lines table whose shaded totals row sums the CURRENT master's lines via the relative sum(G_LINE/AMOUNT) evaluated in the parent context.", "whenToUse": "Invoices, POs, remittances, delivery notes - one document per master node with its own lines and totals. Hierarchies deeper than master>lines escalate to lp:archetype:statement-run / lp:technique:rtf-multilevel-blocks.", "intents": ["classic invoice layout with lines and a total", "one invoice per page with its line items", "po print with header block lines and total per document"], "requires": {"dataShape": "nested output groups G_INVOICE > G_LINE via datamodel groupBy; master fields on the invoice node", "preAggregation": "grand totals ACROSS invoices belong in SQL (ROLLUP/SUM OVER), not the template"}, "composition": {"contains": ["lp:technique:rtf-header-fields-band", "lp:technique:rtf-totals-row"], "notes": "the rtf layout spec's convenience keys (title/headerFields/outerGroup/linesGroup/columns/totals/sort/splitByPage) generate exactly this block tree - prefer them for the plain invoice; drop to blocks[] to add anything beyond the shape"}, "recipe": {"format": "rtf", "blocks": [{"kind": "forEach", "group": "/DATA_DS/G_INVOICE", "splitByPage": true, "blocks": [{"kind": "heading", "level": 1, "runs": ["Invoice ", {"field": "INVOICE_NUM"}]}, {"kind": "paragraph", "runs": [{"text": "Customer: ", "bold": true}, {"field": "CUSTOMER_NAME"}]}, {"kind": "paragraph", "runs": [{"text": "Invoice Date: ", "bold": true}, {"field": "INVOICE_DATE", "format": {"type": "date", "mask": "dd-MMM-yyyy"}}]}, {"kind": "table", "forEach": "G_LINE", "border": true, "headerBg": "#cfe0f1", "totalsBg": "#f7cbac", "columns": [{"header": "Description", "field": "LINE_DESCRIPTION"}, {"header": "Qty", "field": "QTY", "align": "right"}, {"header": "Amount", "field": "AMOUNT", "format": {"type": "number", "mask": "#,##0.00"}, "align": "right"}], "totals": [{"col": 0, "label": "Total", "span": 2}, {"col": 2, "expr": "format-number:sum(G_LINE/AMOUNT);'#,##0.00'"}]}]}]}, "fixtureRef": "fixtures/invoice-shape", "pitfalls": ["The lines path is RELATIVE (G_LINE) - the builder REJECTS an absolute inner group under an outer forEach: every outer iteration would repeat ALL lines and totals would aggregate globally.", "The totals row sits OUTSIDE the for-each (parent context) - sum(G_LINE/AMOUNT) totals the current invoice only; a leading / makes it a document-wide total.", "A totals cell has no format key - to mask the aggregate, make the expr the format tag itself: expr:\"format-number:sum(G_LINE/AMOUNT);'#,##0.00'\" (render-verified); a bare sum() prints 1730.5 unformatted.", "splitByPage on the OUTER forEach = one page per document; on the table block it means one page per LINE - a different technique (lp:technique:rtf-splitbypage-table).", "Bind placeholders (G_INVOICE, INVOICE_NUM) to YOUR datamodel's output tree before building."], "sourceRefs": {"atlas": "repeat-group (#3) + totals-and-aggregates (#16) + page-break-per-group (#6)", "verifiedIn": "shard harness render PASS + PNG check 2026-08-06: two pages, one invoice each, per-invoice totals 1,730.50 and 4,750.00"}, "verified": "render"}
{"id": "lp:technique:rtf-splitbypage-table", "kind": "technique", "name": "One page per repeated ROW (forEach split around a static 1-row table)", "format": "rtf", "dslSupport": "supported", "description": "One printed page per data ROW: a forEach over the row group with splitByPage:true wrapping a STATIC 1-row table - every iteration re-emits the column header plus that row's values, then breaks. This is the render-verified page-per-row shape; the table-level splitByPage key exists but paginates degenerately (see pitfalls).", "whenToUse": "Ticket/voucher/label-per-page output where each ROW is the whole printed unit: delivery tickets, gate passes, one-line confirmations. Every page gets the header row + one record.", "intents": ["one page per ticket row", "print each record on its own page", "page break after every table row"], "requires": {"dataShape": "a flat repeating group where ONE row = one printed unit"}, "composition": {"conflicts": ["forEach splitByPage over a MASTER group (that is page-per-GROUP-with-children - the invoice/statement shape)"], "notes": "the inner table is STATIC (no forEach key) - the wrapping forEach provides the repetition; cell fields resolve in the current row context"}, "recipe": {"blocks": [{"kind": "forEach", "group": "/DATA_DS/G_TICKET", "splitByPage": true, "blocks": [{"kind": "heading", "level": 2, "runs": ["Delivery Ticket ", {"field": "TICKET_NUMBER"}]}, {"kind": "table", "border": true, "headerBg": "#cfe0f1", "columns": [{"header": "Ticket", "field": "TICKET_NUMBER"}, {"header": "Carrier", "field": "CARRIER_NAME"}, {"header": "Weight", "field": "WEIGHT", "align": "right"}]}]}]}, "fixtureRef": "fixtures/split-table", "pitfalls": ["ENGINE QUIRK (render-verified 2026-08-06): splitByPage directly ON a table's row loop does NOT deliver one page per row - the break lands so the header row ends page 1 ALONE and the last two rows share the final page ([header][row1]...[rowN-1 rowN]). Use this forEach + static-1-row-table shape instead - true page-per-row with the header repeated.", "The inner table must be STATIC (no forEach) - adding forEach would nest a second loop inside the row iteration.", "The break tag must stay CONTIGUOUS immediately before <?end for-each?> (the builder places it) - hand-moving it breaks all but the first iteration."], "sourceRefs": {"atlas": "page-break-per-group (#6; split-by-page-break in 203 real files)", "verifiedIn": "split-exp.mts A/B comparison + shard harness render PASS + PNG check 2026-08-06: 3 fixture tickets = 3 pages, each with heading + header row + its own row; table-level split proven degenerate on 4-row data"}, "verified": "render"}
